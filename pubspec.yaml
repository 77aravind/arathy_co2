name: co2_tracker
description: A high-performance Carbon Tracker app.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  percent_indicator: ^4.2.3
  firebase_core: ^3.15.2
  firebase_auth: ^5.0.0
  cloud_firestore: ^5.0.0
  fl_chart: ^0.66.0
  shared_preferences: ^2.2.2
  geolocator: ^14.0.2
  google_mlkit_text_recognition: ^0.15.0
  image_picker: ^1.2.1
  firebase_remote_config: ^5.5.0
  country_picker: ^2.0.20

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
